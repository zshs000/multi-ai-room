{
  "name": "multi-ai-room",
  "version": "0.1.0",
  "description": "多 AI 讨论室：多模型 Agent 同台轮流讨论",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "cd web && npm ci && npm run build",
    "dev:web": "cd web && npm run dev",
    "test": "node test/adapters.test.js && node test/store.test.js && node test/store-io.test.js && node test/orchestrator.test.js && node test/sessions.test.js && node test/typewriter.test.js"
  }
}
